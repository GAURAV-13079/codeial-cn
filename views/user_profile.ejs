<link rel="stylesheet" href="<%= assetPath("css/user_profile.css") %>">

<img src="<%= user.avatar %>" alt="<%= user.name %>" width="100">

<% if (user.id == profile_user.id) { %> 
	<!-- if user mathches then show the form -->
	<form action="/users/update/<%= profile_user.id %>" method="POST" enctype="multipart/form-data">
		<input type="text" name="name" value="<%= user.name %>" placeholder="Your Name" required>
		<input type="email" name="email" value="<%= user.email %>" placeholder="Your Email" required>
		
		<input type="file" name="avatar" placeholder="Profile Picture">
		
		<input type="submit" value="Update">
	</form>
<% } else { %>
	<!-- else show profile info -->
	<p><%= profile_user.name %></p>
	<p><%= profile_user.email %></p>
<% } %>